steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'us-east5-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/samurai-backend:$COMMIT_SHA', '.']
    dir: 'backend'

  # Push the container image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'us-east5-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/samurai-backend:$COMMIT_SHA']

  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'samurai-insurance'
      - '--image'
      - 'us-east5-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/samurai-backend:$COMMIT_SHA'
      - '--region'
      - 'us-east5'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'

images:
  - 'us-east5-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/samurai-backend:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY
